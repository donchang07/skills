# bkit PRD 착수 게이트

목적은 “이 PRD만 받은 bkit이 잘못된 가정 없이 Plan과 Design을 시작할 수 있는가”를 판정하는 것이다. **Hard Gate가 점수보다 우선한다.**

## 파일별 gate 경로

| 대상 | 통과/조건부 gate | draft gate |
|---|---|---|
| `docs/PRD.md` | `docs/00-pm/_product.gate.md` | `docs/00-pm/_product.gate.draft.md` |
| `docs/00-pm/{feature}.prd.md` | `docs/00-pm/{feature}.gate.md` | `docs/00-pm/{feature}.gate.draft.md` |

## 절대 규칙

1. 총평보다 결함을 먼저 쓴다.
2. 모든 결함은 장 번호, FR/SC/결정 ID 또는 표의 행을 인용한다.
3. PRD에 없는 사실을 만들지 않는다. 외부 확인이 필요하면 출처와 기준일을 요구한다.
4. 결함마다 `[작성자]` 또는 `[구현자]` 결정권자를 붙인다.
5. 정본과 파생본의 불일치는 파생본을 임의로 고치지 말고 정본에서 바로잡는다.
6. 점수가 높아도 Hard Gate 실패를 통과로 바꾸지 않는다.

## 1. 기계 검사

스킬 폴더를 기준으로 다음을 실행한다.

```bash
node scripts/validate_prd.mjs <PRD 파일> [추가 PRD 파일...]
```

검사 결과의 `ERROR`는 최소 Major다. ID 중복, 필수 장 누락, unresolved placeholder, 시스템 가정 행 누락은 아래 Hard Gate 규칙에 따라 Blocker가 될 수 있다. `WARN`은 사람이 맥락을 확인한다.

gate 파일에 명령, 종료 코드, ERROR/WARN 개수를 기록한다.

## 2. Hard Gate

다음 중 하나라도 실패하면 Blocker이며 착수 불가다.

| ID | 조건 |
|---|---|
| HG-01 | 정본의 모든 활성 FR/SC ID가 유일하고 feature 분해표에 빠짐없이 연결된다. |
| HG-02 | 모든 P0 FR에 적용 가능한 시나리오·데이터·화면·검증 방법이 연결된다. 적용되지 않는 열은 이유가 있다. |
| HG-03 | 저장·인증·권한·스택 등 P0 아키텍처를 바꾸는 `[결정 필요]` 항목에 안전한 fallback이 없다. |
| HG-04 | feature PRD가 정본과 같은 개정 버전을 가리키며 포함 FR·SC·데이터가 정본과 일치한다. |
| HG-05 | bkit 버전·PRD 탐색 경로·실행 명령이 프로젝트의 로컬 계약 또는 `bkit-contract.md`와 일치한다. |
| HG-06 | 계약 섹션 4~8장에 실행을 멈추는 unresolved placeholder가 남아 있다. |
| HG-07 | 완료 판정에 match rate 기준, SC 통과, 오류 처리 검증이 모두 포함된다. |

## 3. 숫자 재계산

합계·개수·기간·비율·금액·feature별 FR 수를 다시 계산한다. 맞는 값도 기록한다.

| 위치 | 문서 값 | 재계산 | 결과 |
|---|---:|---:|---|

불일치가 범위·비용·납기를 바꾸면 Major, 단순 표기면 Minor다.

## 4. 추적 연결표

| FR | 시나리오 | 화면 | SC | 데이터 | feature | 끊긴 칸 |
|---|---|---|---|---|---|---|

다음을 찾는다.

- 시나리오 없는 FR
- FR이 없는 화면 요소
- 측정할 수 없는 SC
- 부록 A에 없는 데이터
- 어느 feature에도 속하지 않는 활성 FR
- 정본에는 있지만 feature 파생본에 없거나 그 반대인 항목

P0의 연결 단절은 Blocker, P1/P2는 영향에 따라 Major 또는 Minor다.

## 5. 검증 가능성

FR과 SC를 한 줄씩 판정한다.

| 위치 | 판정 가능 여부 | 문제 | 수정안 |
|---|:---:|---|---|

다음이면 판정 불가다.

- 주체·조건·결과 중 하나가 없다.
- 성공과 실패를 나눌 기준이 없다.
- 외부 성과를 소프트웨어가 직접 보장한다고 썼다.
- 데이터나 환경 없이 수치만 있다.

P0 FR 또는 핵심 SC의 판정 불가는 Major이며, 무엇을 만들지 달라지면 Blocker다.

## 6. unresolved 표현 검사

단순한 일반 단어 검색으로 결함을 만들지 않는다. `가능`, `등`, `고려`, `추정`은 금지어가 아니다.

4~8장의 계약 값에서 다음 의미가 남았는지 확인한다.

- `TBD`, `TODO`, `미정`, `택일`, `검토 중`, `결정 필요`
- 채워지지 않은 `{placeholder}` 또는 `<placeholder>`
- “A 또는 B”처럼 bkit이 선택해야 하는 미확정 선택지
- “적절히”, “필요시”, “가능하면”처럼 판정 기준을 숨기는 표현

12·13장의 이슈/결정 표, 인용된 원문, 상태 라벨은 검사 대상에서 제외한다. 계약 섹션에는 확정값 또는 명시적 fallback을 쓰고 논점은 12·13장으로 이동한다.

## 7. 시스템 가정 10칸

11.2의 1~10번 행이 모두 있어야 한다. 각 행은 결정, 상태, 근거를 가진다.

- `[확정]`: 출처 또는 사용자 결정이 있다.
- `[기본값]`: 왜 안전한지와 재확인 시점이 있다.
- `[해당 없음]`: 적용되지 않는 이유가 있다.
- `[결정 필요]`: 13장 ID와 fallback 또는 중단 조건이 연결된다.

빈 행은 Major다. P0 설계가 달라지는 행에 fallback이 없으면 HG-03 Blocker다.

## 8. bkit 소비 가능성

| 항목 | 확인 |
|---|---|
| 0장 Executive Summary 4행과 Context Anchor 5행이 채워졌다. | |
| 각 FR에 우선순위·시나리오·데이터·화면·검증 방법이 있다. | |
| NFR에 기준과 측정 방법이 있다. | |
| 각 SC에 측정 방법과 현재 데이터 검증 가능 여부가 있다. | |
| 7장은 상황·문구·이후 동작·로그 표다. | |
| 각 화면에 요소 목록과 로딩·빈·오류·성공 상태가 있다. | |
| 11장에 레벨·스택과 시스템 가정 10칸이 있다. | |
| 13장의 Open 결정에 fallback 또는 중단 조건이 있다. | |
| 14장의 모든 슬러그가 영문 kebab-case이고 활성 FR을 모두 포함한다. | |
| 15장이 실제 프로젝트의 bkit 계약과 일치한다. | |
| feature 파생본이 정본 개정과 source를 표시한다. | |

빈 항목은 기본적으로 Major이며 Hard Gate와 겹치면 Blocker다.

## 9. 외부 사실

부록 B가 가격·일정·법규·API·외부 서비스 사양을 담았는지 확인한다.

- `[확정·출처]`인데 출처가 없으면 Minor
- 그 사실이 P0 FR의 동작을 바꾸면 Major
- 재확인 시점이 지났고 P0 구현을 바꾸면 Blocker

## 10. 심각도와 점수

- **Blocker −20:** 그대로 시작하면 다른 제품·아키텍처를 만들거나 bkit이 PRD를 찾지 못한다.
- **Major −5:** 만들 수는 있지만 재작업·임의 결정·핵심 검증 누락이 생긴다.
- **Minor −1:** 의미를 바꾸지 않는 참조·표기·가독성 문제다.

100점에서 감점하되 결함을 인위적으로 합치거나 쪼개 점수를 조정하지 않는다.

- 착수 가능: Blocker 0, 90점 이상
- 조건부 착수: Blocker 0, 70~89점
- 착수 불가: Blocker 1 이상 또는 70점 미만

## 11. 다시 읽기

기계 검사와 표를 잠시 내려놓고 PRD를 처음부터 다시 읽는다. 다음 질문에 답한다.

1. bkit이 잘못된 기본값을 선택할 지점이 있는가?
2. 정본에서 feature 파생본으로 내려오며 의미가 사라졌는가?
3. 완료했다고 주장해도 실제로 검증할 수 없는 항목이 있는가?

## gate 파일 구성

1. 대상 파일·정본 개정·검사 시각
2. Hard Gate HG-01~HG-07 결과
3. 점수·판정·한 줄 근거
4. 결함 표: 번호 / 심각도 / 위치 / 문제 / 근거 / 수정안 / 결정권자
5. 기계 검사 결과
6. 숫자 재계산 표
7. 추적 연결표
8. 검증 불가 문장
9. bkit 소비 가능성 표
10. 12장과 13장으로 이동한 항목
